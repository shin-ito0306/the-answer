<% @questions.each do |question| %>
 <%= question.user.name %>
 <%= link_to "#{question.title}", question_path(question.id) %>
 <%= attachment_image_tag question, :question_image %>
 <%= question.content %>
<% end %>