<%= form_with model:@question, local: true do |f| %>
  <h4>題名</h4>
  <%= f.text_field :title %>

  <h4>画像</h4>
  <%= f.attachment_field :question_image %>

  <h4>質問内容</h4>
  <%= f.text_area :content %>
  
  <h4>報酬ポイント</h4>
  <%= f.number_field :reword_point %>

  <%= f.submit "投稿" %>
<% end %>