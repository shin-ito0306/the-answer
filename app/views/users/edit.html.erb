<%= form_with model:@user, local: true do |f| %>
  <h4>名前</h4>
  <%= f.text_field :name %>

  <h4>プロフィール画像</h4>
  <%= f.attachment_field :user_image %>

  <h4>自己紹介</h4>
  <%= f.text_area :introduction %>

  <%= f.submit "投稿" %>
<% end %>